dist: trusty
sudo: false

git:
  depth: 8

branches:
  only:
    - master

language: python

python:
  - "2.7"

install:
  - pip install -U pip
  - pip install -U setuptools
  - pip install -r requirements-ci.txt
  - pip install codecov

script:
  - make cov-dev-full

after_success:
  - codecov
